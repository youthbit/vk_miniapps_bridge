import * as React from 'react';
import { HasComponent } from '../../../types';
export interface CaptionProps extends React.AllHTMLAttributes<HTMLElement>, HasComponent {
    /**
     * Задаёт начертание шрифта отличное от стандартного.
     */
    weight?: '1' | '2' | '3';
    level?: '1' | '2' | '3';
    caps?: boolean;
}
/**
 * @see https://vkcom.github.io/VKUI/#/Caption
 */
export declare const Caption: ({ className, children, weight, level, caps, Component, ...restProps }: CaptionProps) => JSX.Element;
