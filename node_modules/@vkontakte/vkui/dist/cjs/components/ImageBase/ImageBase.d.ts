import * as React from 'react';
import type { HasRef, HasRootRef } from '../../types';
import { type ImageBaseBadgeProps } from './ImageBaseBadge/ImageBaseBadge';
import { type ImageBaseOverlayProps } from './ImageBaseOverlay/ImageBaseOverlay';
import type { ImageBaseExpectedIconProps, ImageBaseSize, ImageBaseContextProps } from './types';
import { ImageBaseContext } from './context';
export type { ImageBaseSize, ImageBaseExpectedIconProps, ImageBaseBadgeProps, ImageBaseOverlayProps, ImageBaseContextProps, };
export { getBadgeIconSizeByImageBaseSize, getFallbackIconSizeByImageBaseSize, getOverlayIconSizeByImageBaseSize, } from './helpers';
export { ImageBaseContext };
export interface ImageBaseProps extends React.ImgHTMLAttributes<HTMLElement>, HasRootRef<HTMLDivElement>, HasRef<HTMLImageElement> {
    /**
     * Задаёт размер картинки.
     *
     * Используйте размеры заданные дизайн-системой `16 | 20 | 24 | 28 | 32 | 36 | 40 | 44 | 48 | 56 | 64 | 72 | 80 | 88 | 96`.
     *
     * > ⚠️ Использование кастомного размера – это пограничный кейс.
     */
    size?: ImageBaseSize | number;
    /**
     * Включает или отключает обводку.
     */
    withBorder?: boolean;
    /**
     * Фолбек на случай, если картинка не прогрузилась.
     *
     * > 📝 Нужный для `<ImageBase size={...} />` размер можно узнать из функции `getFallbackIconSizeByImageBaseSize()`.
     *
     * > Предпочтительней использовать иконки из `@vkontakte/icons`.
     *
     * > 📊️ Если вы хотите передать кастомную иконку, то следует именовать её по шаблону `Icon<size><name>`. Или же
     * > чтобы в неё был передан параметр `width`. Тогда мы сможем выводить в консоль подсказку правильного ли размера вы
     * > использовали иконку.
     *
     * > ⚠️ Может перекрывать `children`.
     */
    fallbackIcon?: React.ReactElement<ImageBaseExpectedIconProps>;
}
/**
 * @see https://vkcom.github.io/VKUI/#/ImageBase
 */
export declare const ImageBase: {
    ({ alt, crossOrigin, decoding, loading, referrerPolicy, sizes, src, srcSet, useMap, getRef, size, width, height, style, className, getRootRef, withBorder, "fallbackIcon": fallbackIconProp, children, "aria-label": ariaLabel, onClick, onLoad, onError, ...restProps }: ImageBaseProps): JSX.Element;
    Badge: ({ background, children, className, ...restProps }: ImageBaseBadgeProps) => JSX.Element;
    Overlay: ({ className, theme: themeProp, visibility: visibilityProp, children, onClick, ...restProps }: ImageBaseOverlayProps) => JSX.Element;
};
