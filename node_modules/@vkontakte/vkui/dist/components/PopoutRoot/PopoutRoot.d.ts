import * as React from 'react';
import { HasRootRef } from '../../types';
export interface PopoutRootProps extends React.HTMLAttributes<HTMLDivElement>, HasRootRef<HTMLDivElement> {
    popout?: React.ReactNode;
    modal?: React.ReactNode;
}
export declare const PopoutRoot: ({ popout, modal, children, getRootRef, className, ...restProps }: PopoutRootProps) => JSX.Element;
