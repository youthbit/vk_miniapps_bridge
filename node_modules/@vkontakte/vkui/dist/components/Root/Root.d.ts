import * as React from 'react';
import { NavIdProps } from '../../lib/getNavId';
export interface RootProps extends React.HTMLAttributes<HTMLDivElement>, NavIdProps {
    activeView: string;
    onTransition?(params: {
        isBack: boolean;
        from: string;
        to: string;
    }): void;
}
export interface RootState {
    activeView: string;
    transition: boolean;
    isBack?: boolean;
    prevView?: string;
}
/**
 * @see https://vkcom.github.io/VKUI/#/Root
 */
export declare const Root: ({ children, activeView: _activeView, onTransition, nav, className, ...restProps }: RootProps) => JSX.Element;
