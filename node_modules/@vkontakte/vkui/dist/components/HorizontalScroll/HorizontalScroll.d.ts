import * as React from 'react';
import { HasRef } from '../../types';
export declare type ScrollPositionHandler = (currentPosition: number) => number;
export interface HorizontalScrollProps extends React.HTMLAttributes<HTMLDivElement>, HasRef<HTMLDivElement> {
    /**
     * Функция для расчета величины прокрутки при клике на левую стрелку.
     */
    getScrollToLeft?: ScrollPositionHandler;
    /**
     * Функция для расчета величины прокрутки при клике на правую стрелку.
     */
    getScrollToRight?: ScrollPositionHandler;
    arrowSize?: 'm' | 'l';
    showArrows?: boolean | 'always';
    scrollAnimationDuration?: number;
}
/**
 * @see https://vkcom.github.io/VKUI/#/HorizontalScroll
 */
export declare const HorizontalScroll: ({ children, getScrollToLeft, getScrollToRight, showArrows, arrowSize, scrollAnimationDuration, getRef, className, ...restProps }: HorizontalScrollProps) => JSX.Element;
