import * as React from 'react';
import { HasRootRef } from '../../types';
export interface TabsProps extends React.HTMLAttributes<HTMLDivElement>, HasRootRef<HTMLDivElement> {
    mode?: 'default' | 'accent' | 'secondary';
}
export interface TabsContextProps {
    mode: TabsProps['mode'];
    withGaps: boolean;
}
export declare const TabsModeContext: React.Context<TabsContextProps>;
/**
 * @see https://vkcom.github.io/VKUI/#/Tabs
 */
export declare const Tabs: ({ children, mode, getRootRef, className, role, ...restProps }: TabsProps) => JSX.Element;
