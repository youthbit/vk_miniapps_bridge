import * as React from 'react';
import { TouchEvent } from '../Touch/Touch';
import { HasRootRef } from '../../types';
export declare type UniversalValue = [number | null, number];
export interface UniversalSliderProps<Value> extends HasRootRef<HTMLDivElement>, Omit<React.HTMLAttributes<HTMLDivElement>, 'value' | 'defaultValue' | 'onChange'> {
    min?: number;
    max?: number;
    step?: number;
    value?: Value;
    defaultValue?: Value;
    disabled?: boolean;
    onChange?(value: Value, e: TouchEvent): void;
}
export declare const UniversalSlider: ({ min, max, step, value, defaultValue, onChange, getRootRef, disabled, className, ...restProps }: UniversalSliderProps<UniversalValue>) => JSX.Element;
