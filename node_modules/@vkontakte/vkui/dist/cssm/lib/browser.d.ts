import type { Version } from '../types';
export declare enum System {
    IOS = "ios",
    UNKNOWN = ""
}
export interface BrowserInfo {
    userAgent: string;
    system: System;
    systemVersion: Version | null;
}
export declare function computeBrowserInfo(userAgent?: string): BrowserInfo;
/**
 * Эмуляция функции `window.matchMedia` для SSR.
 *
 * ⚠️ Желательно избегать использование этой эмуляции в SSR.
 */
export declare function mediaQueryNull(query: string): MediaQueryList;
